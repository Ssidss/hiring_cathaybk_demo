

spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: 'sa'
    password:
    url: 'jdbc:h2:mem:cathaybk_demo'
  h2:
    console:
      enabled: true
      path: '/h2'
  jpa:
    generate-ddl: false
    open-in-view: true
#    hibernate:
#     ddl-auto: update  # ? spring boot entity ????, ????? DB scheam
